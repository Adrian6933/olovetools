// Unified Chrome — same header/footer across all tools from CompressSnap onwards.
// Modify these files once and every tool that uses them updates together.
export { UnifiedHeader } from './UnifiedHeader';
export { UnifiedFooter } from './UnifiedFooter';
export type { UnifiedFooterContent } from './UnifiedFooter';
