---
import { Clipy as ClipyApp } from '../../../tools/clipy/Clipy';
import Twitchbolt from '../../../tools/twitchbolt/Twitchbolt';
import Kickbolt from '../../../tools/kickbolt/Kickbolt';
import Formatflow from '../../../tools/formatflow/Formatflow';
import Pastesnap from '../../../tools/pastesnap/Pastesnap';
import Compresssnap from '../../../tools/compresssnap/Compresssnap';
import Backgroundremover from '../../../tools/backgroundremover/Backgroundremover';
import Pdfflow from '../../../tools/pdf-flow/Pdfflow';
import Recordsnap from '../../../tools/recordsnap/Recordsnap';
import Qrbolt from '../../../tools/qr-bolt/Qrbolt';
import Codecard from '../../../tools/codecard/Codecard';
import Cropsnap from '../../../tools/cropsnap/Cropsnap';
import CSSDesigner from '../../../tools/css-designer/CSSDesigner';
import { JSONFlow } from '../../../tools/json-flow/JSONFlow';
import { SocialBolt } from '../../../tools/socialbolt/SocialBolt';
import TTSBolt from '../../../tools/tts-bolt/TTSBolt';
import GIFBolt from '../../../tools/gif-bolt/GIFBolt';
import { CookieBanner } from '../../../components/CookieBanner';
import { ToolPlaceholder } from '../../../components/ToolPlaceholder';
import { useTranslation, Language } from '../../../locales/dictionary';
import { ClientRouter } from 'astro:transitions';
import { TOOL_FAVICONS, TOOL_THEME_COLORS } from '../../../constants';
import '../../../styles/global.css';

export function getStaticPaths() {
  const tools = ['clipy', 'twitchbolt', 'kickbolt', 'formatflow', 'pastesnap', 'compresssnap', 'backgroundremover', 'pdf-flow', 'recordsnap', 'qr-bolt', 'codecard', 'cropsnap', 'css-designer', 'json-flow', 'socialbolt', 'tts-bolt', 'gif-bolt'];
  const languages = ['en', 'es', 'fr', 'de', 'pt', 'ru', 'hi', 'ja', 'zh'];
  
  return tools.flatMap(tool => 
    languages.map(lang => ({
      params: { lang, tool },
    }))
  );
}

const { lang, tool } = Astro.params;
const castLang = lang as Language;
const { t, dictionary } = useTranslation(castLang, tool as any);

const langs = ['en', 'es', 'fr', 'de', 'pt', 'ru', 'hi', 'ja', 'zh'];
const baseUrl = 'https://olovetools.com';

const currentFavicon = TOOL_FAVICONS[tool] || TOOL_FAVICONS.default;
const currentThemeColor = TOOL_THEME_COLORS[tool] || TOOL_THEME_COLORS.default;

// Standardize title: ToolName | Short Description
const toolNames: Record<string, string> = {
  clipy: 'Clipy',
  twitchbolt: 'ClipBolt',
  kickbolt: 'Kickbolt',
  formatflow: 'FormatFlow',
  pastesnap: 'PasteSnap',
  compresssnap: 'CompressSnap',
  backgroundremover: 'Background Remover',
  'pdf-flow': 'PDF-Flow',
  recordsnap: 'RecordSnap',
  'qr-bolt': 'QR-Bolt',
  codecard: 'CodeCard',
  cropsnap: 'CropSnap',
  'css-designer': 'CSS-Designer',
  'json-flow': 'JSON-Flow',
  'socialbolt': 'SocialBolt',
  'tts-bolt': 'TTS-Bolt',
  'gif-bolt': 'GIF-Bolt'
};
const name = toolNames[tool as string] || ((tool as string).charAt(0).toUpperCase() + (tool as string).slice(1));
let seoTitle = t('seo_title');
seoTitle = seoTitle.replace(/^oLoveTools\s*[|\-]\s*/gi, '');
const finalTitle = seoTitle.toLowerCase().includes(name.toLowerCase()) 
  ? seoTitle 
  : `${name} | ${seoTitle}`;

---

<html lang={lang}>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{finalTitle}</title>
    <meta property="og:title" content={finalTitle} />
    <meta name="twitter:title" content={finalTitle} />
    <ClientRouter />
    <meta name="description" content={t('seo_description')} />
    <meta property="og:description" content={t('seo_description')} />
    <meta property="og:url" content={`${baseUrl}/${lang}/${tool}`} />
    <meta property="og:type" content="website" />
    <meta property="og:image" content={`${baseUrl}/og-${tool}.png`} />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:description" content={t('seo_description')} />
    <meta name="twitter:image" content={`${baseUrl}/og-${tool}.png`} />
    <link rel="canonical" href={`${baseUrl}/${lang}/${tool}`} />
    <meta name="google-adsense-account" content="ca-pub-4601581729676999">
    <script async src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-4601581729676999" crossorigin="anonymous"></script>
    
    <link rel="icon" type={currentFavicon.endsWith('.svg') ? 'image/svg+xml' : 'image/png'} href={currentFavicon}>
    <link rel="apple-touch-icon" href="/icon.svg">
    <link rel="manifest" href="/manifest.json">
    <meta name="theme-color" content={currentThemeColor} />
    
    <!-- Preconnect to Google Fonts for faster font loading -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&family=Outfit:wght@300;400;500;600;700;800&display=swap" rel="stylesheet" media="print" onload="this.media='all'" />
    <noscript><link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&family=Outfit:wght@300;400;500;600;700;800&display=swap" rel="stylesheet"></noscript>
    
    {tool === 'clipy' && (
      <>
        <link rel="preconnect" href="https://api.twitch.tv" crossorigin />
        <link rel="preconnect" href="https://id.twitch.tv" crossorigin />
        <link rel="preconnect" href="https://static-cdn.jtvnw.net" />
      </>
    )}

    {langs.map(l => (
      <link rel="alternate" hreflang={l} href={`${baseUrl}/${l}/${tool}`} />
    ))}
    <link rel="alternate" hreflang="x-default" href={`${baseUrl}/en/${tool}`} />

    <script type="application/ld+json" is:inline set:html={JSON.stringify({
      "@context": "https://schema.org",
      "@type": "WebApplication",
      "name": `oLoveTools - ${tool}`,
      "applicationCategory": "UtilitiesApplication",
      "operatingSystem": "All",
      "description": t('seo_description'),
      "url": `${baseUrl}/${lang}/${tool}`
    })} />
    {tool === 'clipy' && (
      <style is:inline>
        body { 
          background: radial-gradient(circle at top center, #100a1f 0%, #050507 100%) !important;
          background-attachment: fixed !important;
          color: #efeff1 !important; 
          font-family: 'Plus Jakarta Sans', sans-serif !important; 
          overflow-x: hidden !important;
          width: 100% !important;
        }
        body::before {
          content: "";
          position: fixed;
          top: 0; left: 0; width: 100%; height: 100%;
          background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noiseFilter'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.65' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noiseFilter)'/%3E%3C/svg%3E");
          opacity: 0.01;
          pointer-events: none;
          z-index: 100;
        }
        #root { min-height: 100vh; position: relative; z-index: 10; }
        ::-webkit-scrollbar { width: 8px; }
        ::-webkit-scrollbar-track { background: #050507; }
        ::-webkit-scrollbar-thumb { background: rgba(145, 70, 255, 0.4); border-radius: 10px; border: 2px solid #050507; }
        ::-webkit-scrollbar-thumb:hover { background: rgba(145, 70, 255, 0.6); }

        /* ANIMACIONES EXCLUSIVAS PARA CLIPY */
        @keyframes rotate-continuous { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
        .animate-rotate-slow { animation: rotate-continuous 12s linear infinite; }
        .transition-premium { transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1); }

        .custom-scrollbar::-webkit-scrollbar { height: 6px; }
        .custom-scrollbar::-webkit-scrollbar-track { background: transparent; }
        .custom-scrollbar::-webkit-scrollbar-thumb { background: rgba(255, 255, 255, 0.1); border-radius: 10px; }
        .custom-scrollbar::-webkit-scrollbar-thumb:hover { background: rgba(145, 70, 255, 0.5); }
        .custom-scrollbar { scrollbar-width: thin; scrollbar-color: rgba(255, 255, 255, 0.1) transparent; }
        
        .glass {
          background: rgba(255, 255, 255, 0.03);
          backdrop-filter: blur(10px);
          -webkit-backdrop-filter: blur(10px);
        }
      </style>
    )}

    {tool === 'twitchbolt' && (
      <style is:inline>
        body {
          background-color: #070708 !important;
          background-image: radial-gradient(circle at 50% -25%, #110b22 0%, #070708 50%) !important;
          background-attachment: fixed !important;
          color: #efeff1 !important;
          font-family: 'Outfit', sans-serif !important;
          min-height: 100vh !important;
          overflow-x: hidden !important;
          width: 100% !important;
        }
        #root { min-height: 100vh; position: relative; z-index: 10; }
        ::-webkit-scrollbar { width: 10px; }
        ::-webkit-scrollbar-track { background: #0e0e10; }
        ::-webkit-scrollbar-thumb { background: #2f2f35; border-radius: 5px; border: 2px solid #0e0e10; }
        ::-webkit-scrollbar-thumb:hover { background: #9146FF; }
        
        .glass-card {
          background: rgba(24, 24, 27, 0.6);
          backdrop-filter: blur(12px);
          border: 1px solid rgba(255, 255, 255, 0.05);
        }
        .scrollbar-hide::-webkit-scrollbar {
          display: none;
        }
        .scrollbar-hide {
          -ms-overflow-style: none;
          scrollbar-width: none;
        }
      </style>
    )}

    {tool === 'kickbolt' && (
      <style is:inline>
        body {
          background-color: #070708 !important;
          background-image: radial-gradient(circle at 50% -25%, #0a1c0a 0%, #070708 50%) !important;
          background-attachment: fixed !important;
          color: #efeff1 !important;
          font-family: 'Outfit', sans-serif !important;
          min-height: 100vh !important;
          overflow-x: hidden !important;
          width: 100% !important;
        }
        #root { min-height: 100vh; position: relative; z-index: 10; }
        ::-webkit-scrollbar { width: 10px; }
        ::-webkit-scrollbar-track { background: #0e0e10; }
        ::-webkit-scrollbar-thumb { background: #2f2f35; border-radius: 5px; border: 2px solid #0e0e10; }
        ::-webkit-scrollbar-thumb:hover { background: #53FC18; }
        
        .glass-card {
          background: rgba(24, 24, 27, 0.6);
          backdrop-filter: blur(12px);
          border: 1px solid rgba(255, 255, 255, 0.05);
        }
        .scrollbar-hide::-webkit-scrollbar {
          display: none;
        }
        .scrollbar-hide {
          -ms-overflow-style: none;
          scrollbar-width: none;
        }
      </style>
    )}

    {tool === 'formatflow' && (
      <style is:inline>
        body {
          background-color: #080c14 !important;
          color: #e2e8f0 !important;
          min-height: 100vh !important;
          overflow-x: hidden !important;
          width: 100% !important;
        }
        #root { min-height: 100vh; position: relative; z-index: 10; }
        ::-webkit-scrollbar { width: 6px; }
        ::-webkit-scrollbar-track { background: #080c14; }
        ::-webkit-scrollbar-thumb { background: #312e81; border-radius: 10px; }
        ::-webkit-scrollbar-thumb:hover { background: #4338ca; }
        
        .glass-card {
          background: rgba(17, 24, 39, 0.4);
          backdrop-filter: blur(16px);
          -webkit-backdrop-filter: blur(16px);
          border: 1px solid rgba(255, 255, 255, 0.08);
          box-shadow: 0 4px 30px rgba(0, 0, 0, 0.1);
        }
        
        input[type=range] { -webkit-appearance: none; background: transparent; }
        input[type=range]::-webkit-slider-thumb {
          -webkit-appearance: none; height: 20px; width: 20px; border-radius: 50%;
          background: #ffffff; cursor: pointer; margin-top: -9px;
          box-shadow: 0 0 10px rgba(0,0,0,0.5); border: 2px solid currentColor;
        }
        input[type=range]::-webkit-slider-runnable-track {
          width: 100%; height: 4px; cursor: pointer;
          background: rgba(255,255,255,0.1); border-radius: 2px;
        }
        
        .stagger-1 { animation-delay: 0.1s; }
        .stagger-2 { animation-delay: 0.2s; }
        .stagger-3 { animation-delay: 0.3s; }
        
        .glow-primary { box-shadow: 0 0 20px -5px rgba(99, 102, 241, 0.3); }
        .glow-secondary { box-shadow: 0 0 20px -5px rgba(168, 85, 247, 0.3); }
      </style>
    )}

    {tool === 'pastesnap' && (
      <style is:inline>
        body { margin: 0; background-color: #04050a !important; color: #f3f4f6 !important; min-height: 100vh !important; font-family: 'Plus Jakarta Sans', sans-serif !important; overflow-x: hidden !important; width: 100% !important; }
        ::-webkit-scrollbar { width: 8px; }
        ::-webkit-scrollbar-track { background: #04050a; }
        ::-webkit-scrollbar-thumb { background: #4f46e5; border-radius: 4px; }
        ::-webkit-scrollbar-thumb:hover { background: #6366f1; }
        html { scroll-behavior: smooth; }
        .glass-card { background: rgba(255, 255, 255, 0.03); backdrop-filter: blur(20px); -webkit-backdrop-filter: blur(20px); border: 1px solid rgba(255, 255, 255, 0.05); }
        .text-glow { text-shadow: 0 0 30px rgba(99, 102, 241, 0.6), 0 0 60px rgba(79, 70, 229, 0.4); }
        @keyframes custom-pulse { 0%, 100% { opacity: 1; transform: scale(1); } 50% { opacity: 0.8; transform: scale(1.05); } }
        .animate-fast-pulse { animation: custom-pulse 8s ease-in-out infinite; }
        @keyframes float { 0%, 100% { transform: translateY(0); } 50% { transform: translateY(-10px); } }
        .animate-float { animation: float 6s ease-in-out infinite; }
      </style>
    )}

    {tool === 'compresssnap' && (
      <style is:inline>
        body {
          background-color: #060b11 !important;
          color: #e2e8f0 !important;
          min-height: 100vh !important;
          overflow-x: hidden !important;
          width: 100% !important;
        }
        #root { min-height: 100vh; position: relative; z-index: 10; }
        ::-webkit-scrollbar { width: 6px; }
        ::-webkit-scrollbar-track { background: #060b11; }
        ::-webkit-scrollbar-thumb { background: #0f766e; border-radius: 10px; }
        ::-webkit-scrollbar-thumb:hover { background: #0d9488; }
        
        .glass-card {
          background: rgba(15, 23, 42, 0.45);
          backdrop-filter: blur(16px);
          -webkit-backdrop-filter: blur(16px);
          border: 1px solid rgba(255, 255, 255, 0.07);
          box-shadow: 0 4px 30px rgba(0, 0, 0, 0.1);
        }
        
        input[type=range] { -webkit-appearance: none; background: transparent; }
        input[type=range]::-webkit-slider-thumb {
          -webkit-appearance: none; height: 18px; width: 18px; border-radius: 50%;
          background: #ffffff; cursor: pointer; margin-top: -8px;
          box-shadow: 0 0 10px rgba(0,0,0,0.5); border: 2px solid #06b6d4;
        }
        input[type=range]::-webkit-slider-runnable-track {
          width: 100%; height: 4px; cursor: pointer;
          background: rgba(255,255,255,0.1); border-radius: 2px;
        }
        
        .stagger-1 { animation-delay: 0.1s; }
        .stagger-2 { animation-delay: 0.2s; }
        .stagger-3 { animation-delay: 0.3s; }
        
        .glow-cyan { box-shadow: 0 0 25px -5px rgba(6, 182, 212, 0.35); }
      </style>
    )}

    {tool === 'backgroundremover' && (
      <style is:inline>
        body {
          background-color: #08060b !important;
          color: #e2e8f0 !important;
          min-height: 100vh !important;
          overflow-x: hidden !important;
          width: 100% !important;
        }
        #root { min-height: 100vh; position: relative; z-index: 10; }
        ::-webkit-scrollbar { width: 6px; }
        ::-webkit-scrollbar-track { background: #08060b; }
        ::-webkit-scrollbar-thumb { background: #701a75; border-radius: 10px; }
        ::-webkit-scrollbar-thumb:hover { background: #86198f; }
        
        .glass-card {
          background: rgba(15, 10, 25, 0.45);
          backdrop-filter: blur(16px);
          -webkit-backdrop-filter: blur(16px);
          border: 1px solid rgba(255, 255, 255, 0.06);
          box-shadow: 0 4px 30px rgba(0, 0, 0, 0.1);
        }

        .checkered-bg {
          background-image: linear-gradient(45deg, #110e19 25%, transparent 25%),
                            linear-gradient(-45deg, #110e19 25%, transparent 25%),
                            linear-gradient(45deg, transparent 75%, #110e19 75%),
                            linear-gradient(-45deg, transparent 75%, #110e19 75%);
          background-size: 20px 20px;
          background-position: 0 0, 0 10px, 10px -10px, -10px 0px;
          background-color: #060408;
        }

        input[type=range] { -webkit-appearance: none; background: transparent; }
        input[type=range]::-webkit-slider-thumb {
          -webkit-appearance: none; height: 18px; width: 18px; border-radius: 50%;
          background: #ffffff; cursor: pointer; margin-top: -8px;
          box-shadow: 0 0 10px rgba(0,0,0,0.5); border: 2px solid #ec4899;
        }
        input[type=range]::-webkit-slider-runnable-track {
          width: 100%; height: 4px; cursor: pointer;
          background: rgba(255,255,255,0.1); border-radius: 2px;
        }
        
        .stagger-1 { animation-delay: 0.1s; }
        .stagger-2 { animation-delay: 0.2s; }
        .stagger-3 { animation-delay: 0.3s; }
        
        .glow-fuchsia { box-shadow: 0 0 25px -5px rgba(236, 72, 153, 0.35); }
      </style>
    )}

    {tool === 'pdf-flow' && (
      <style is:inline>
        body {
          background-color: #0c0506 !important;
          background-image: radial-gradient(circle at 50% -25%, #2a0b0e 0%, #0c0506 50%) !important;
          background-attachment: fixed !important;
          color: #e2e8f0 !important;
          min-height: 100vh !important;
          overflow-x: hidden !important;
          width: 100% !important;
        }
        #root { min-height: 100vh; position: relative; z-index: 10; }
        ::-webkit-scrollbar { width: 6px; }
        ::-webkit-scrollbar-track { background: #0c0506; }
        ::-webkit-scrollbar-thumb { background: #991b1b; border-radius: 10px; }
        ::-webkit-scrollbar-thumb:hover { background: #b91c1c; }
        
        .glass-card {
          background: rgba(20, 10, 12, 0.45);
          backdrop-filter: blur(16px);
          -webkit-backdrop-filter: blur(16px);
          border: 1px solid rgba(255, 255, 255, 0.06);
          box-shadow: 0 4px 30px rgba(0, 0, 0, 0.1);
        }

        input[type=range] { -webkit-appearance: none; background: transparent; }
        input[type=range]::-webkit-slider-thumb {
          -webkit-appearance: none; height: 18px; width: 18px; border-radius: 50%;
          background: #ffffff; cursor: pointer; margin-top: -8px;
          box-shadow: 0 0 10px rgba(0,0,0,0.5); border: 2px solid #ef4444;
        }
        input[type=range]::-webkit-slider-runnable-track {
          width: 100%; height: 4px; cursor: pointer;
          background: rgba(255,255,255,0.1); border-radius: 2px;
        }
        
        .stagger-1 { animation-delay: 0.1s; }
        .stagger-2 { animation-delay: 0.2s; }
        .stagger-3 { animation-delay: 0.3s; }
        
        .glow-red { box-shadow: 0 0 25px -5px rgba(239, 68, 68, 0.35); }
      </style>
    )}

    {tool === 'recordsnap' && (
      <style is:inline>
        body {
          background-color: #0b0805 !important;
          background-image: radial-gradient(circle at 50% -25%, #2a1b0a 0%, #0b0805 50%) !important;
          background-attachment: fixed !important;
          color: #e2e8f0 !important;
          min-height: 100vh !important;
          overflow-x: hidden !important;
          width: 100% !important;
        }
        #root { min-height: 100vh; position: relative; z-index: 10; }
        ::-webkit-scrollbar { width: 6px; }
        ::-webkit-scrollbar-track { background: #0b0805; }
        ::-webkit-scrollbar-thumb { background: #d97706; border-radius: 10px; }
        ::-webkit-scrollbar-thumb:hover { background: #f59e0b; }
        
        .glass-card {
          background: rgba(20, 15, 10, 0.45);
          backdrop-filter: blur(16px);
          -webkit-backdrop-filter: blur(16px);
          border: 1px solid rgba(255, 255, 255, 0.06);
          box-shadow: 0 4px 30px rgba(0, 0, 0, 0.1);
        }
        
        .glow-amber { box-shadow: 0 0 25px -5px rgba(245, 158, 11, 0.35); }
      </style>
    )}

    {tool === 'qr-bolt' && (
      <style is:inline>
        body {
          background-color: #050907 !important;
          background-image: radial-gradient(circle at 50% -25%, #092317 0%, #050907 50%) !important;
          background-attachment: fixed !important;
          color: #e2e8f0 !important;
          min-height: 100vh !important;
          overflow-x: hidden !important;
          width: 100% !important;
        }
        #root { min-height: 100vh; position: relative; z-index: 10; }
        ::-webkit-scrollbar { width: 6px; }
        ::-webkit-scrollbar-track { background: #050907; }
        ::-webkit-scrollbar-thumb { background: #059669; border-radius: 10px; }
        ::-webkit-scrollbar-thumb:hover { background: #10b981; }
        
        .glass-card {
          background: rgba(10, 20, 15, 0.45);
          backdrop-filter: blur(16px);
          -webkit-backdrop-filter: blur(16px);
          border: 1px solid rgba(255, 255, 255, 0.06);
          box-shadow: 0 4px 30px rgba(0, 0, 0, 0.1);
        }
        
        .glow-emerald { box-shadow: 0 0 25px -5px rgba(16, 185, 129, 0.35); }
      </style>
    )}

    {tool === 'codecard' && (
      <style is:inline>
        body {
          background-color: #06050a !important;
          background-image: radial-gradient(circle at 50% -25%, #160e29 0%, #06050a 50%) !important;
          background-attachment: fixed !important;
          color: #e2e8f0 !important;
          min-height: 100vh !important;
          overflow-x: hidden !important;
          width: 100% !important;
        }
        #root { min-height: 100vh; position: relative; z-index: 10; }
        ::-webkit-scrollbar { width: 6px; }
        ::-webkit-scrollbar-track { background: #06050a; }
        ::-webkit-scrollbar-thumb { background: #4f46e5; border-radius: 10px; }
        ::-webkit-scrollbar-thumb:hover { background: #6366f1; }
        
        .glass-card {
          background: rgba(10, 8, 18, 0.45);
          backdrop-filter: blur(16px);
          -webkit-backdrop-filter: blur(16px);
          border: 1px solid rgba(255, 255, 255, 0.06);
          box-shadow: 0 4px 30px rgba(0, 0, 0, 0.1);
        }
        
        .glow-indigo { box-shadow: 0 0 25px -5px rgba(99, 102, 241, 0.35); }
      </style>
    )}

    {tool === 'cropsnap' && (
      <style is:inline>
        body {
          background-color: #090506 !important;
          background-image: radial-gradient(circle at 50% -25%, #290e13 0%, #090506 50%) !important;
          background-attachment: fixed !important;
          color: #e2e8f0 !important;
          min-height: 100vh !important;
          overflow-x: hidden !important;
          width: 100% !important;
        }
        #root { min-height: 100vh; position: relative; z-index: 10; }
        ::-webkit-scrollbar { width: 6px; }
        ::-webkit-scrollbar-track { background: #090506; }
        ::-webkit-scrollbar-thumb { background: #e11d48; border-radius: 10px; }
        ::-webkit-scrollbar-thumb:hover { background: #f43f5e; }
        
        .glass-card {
          background: rgba(20, 10, 12, 0.45);
          backdrop-filter: blur(16px);
          -webkit-backdrop-filter: blur(16px);
          border: 1px solid rgba(255, 255, 255, 0.06);
          box-shadow: 0 4px 30px rgba(0, 0, 0, 0.1);
        }
        
        .glow-rose { box-shadow: 0 0 25px -5px rgba(244, 63, 94, 0.35); }
      </style>
    )}

    {tool === 'css-designer' && (
      <style is:inline>
        body {
          background-color: #07060b !important;
          background-image: radial-gradient(circle at 50% -20%, #1c0e35 0%, #07060b 60%) !important;
          background-attachment: fixed !important;
          color: #e2e8f0 !important;
          min-height: 100vh !important;
          overflow-x: hidden !important;
          width: 100% !important;
        }
        #root { min-height: 100vh; position: relative; z-index: 10; }
        ::-webkit-scrollbar { width: 6px; }
        ::-webkit-scrollbar-track { background: #07060b; }
        ::-webkit-scrollbar-thumb { background: #5c3eab; border-radius: 10px; }
        ::-webkit-scrollbar-thumb:hover { background: #7c3aed; }
        
        .glass-card {
          background: rgba(15, 10, 25, 0.45);
          backdrop-filter: blur(16px);
          -webkit-backdrop-filter: blur(16px);
          border: 1px solid rgba(255, 255, 255, 0.06);
          box-shadow: 0 4px 30px rgba(0, 0, 0, 0.1);
        }
        
        .glow-violet { box-shadow: 0 0 25px -5px rgba(139, 92, 246, 0.35); }
      </style>
    )}

    {tool === 'json-flow' && (
      <style is:inline>
        body {
          background-color: #050807 !important;
          background-image: radial-gradient(circle at 50% -20%, #08251b 0%, #050807 60%) !important;
          background-attachment: fixed !important;
          color: #e2e8f0 !important;
          min-height: 100vh !important;
          overflow-x: hidden !important;
          width: 100% !important;
        }
        #root { min-height: 100vh; position: relative; z-index: 10; }
        ::-webkit-scrollbar { width: 6px; }
        ::-webkit-scrollbar-track { background: #050807; }
        ::-webkit-scrollbar-thumb { background: #0b6146; border-radius: 10px; }
        ::-webkit-scrollbar-thumb:hover { background: #10b981; }
        
        .glass-card {
          background: rgba(10, 15, 12, 0.45);
          backdrop-filter: blur(16px);
          -webkit-backdrop-filter: blur(16px);
          border: 1px solid rgba(255, 255, 255, 0.06);
          box-shadow: 0 4px 30px rgba(0, 0, 0, 0.1);
        }
        
        .glow-emerald { box-shadow: 0 0 25px -5px rgba(16, 185, 129, 0.35); }
      </style>
    )}

    {tool === 'socialbolt' && (
      <style is:inline>
        body {
          background-color: #050508 !important;
          background-attachment: fixed !important;
          color: #e2e8f0 !important;
          min-height: 100vh !important;
          overflow-x: hidden !important;
          width: 100% !important;
        }
        #root { min-height: 100vh; position: relative; z-index: 10; }
        ::-webkit-scrollbar { width: 6px; }
        ::-webkit-scrollbar-track { background: #050508; }
        ::-webkit-scrollbar-thumb { background: #312e81; border-radius: 10px; }
        ::-webkit-scrollbar-thumb:hover { background: #4f46e5; }
        
        .glass-card {
          background: rgba(10, 10, 15, 0.45);
          backdrop-filter: blur(16px);
          -webkit-backdrop-filter: blur(16px);
          border: 1px solid rgba(255, 255, 255, 0.06);
          box-shadow: 0 4px 30px rgba(0, 0, 0, 0.1);
        }
      </style>
    )}

    {tool === 'tts-bolt' && (
      <style is:inline>
        body {
          background-color: #060504 !important;
          background-image: radial-gradient(circle at 50% -25%, #2a180a 0%, #060504 50%) !important;
          background-attachment: fixed !important;
          color: #e2e8f0 !important;
          min-height: 100vh !important;
          overflow-x: hidden !important;
          width: 100% !important;
        }
        #root { min-height: 100vh; position: relative; z-index: 10; }
        ::-webkit-scrollbar { width: 6px; }
        ::-webkit-scrollbar-track { background: #060504; }
        ::-webkit-scrollbar-thumb { background: #d97706; border-radius: 10px; }
        ::-webkit-scrollbar-thumb:hover { background: #f59e0b; }
        
        .glass-card {
          background: rgba(20, 12, 10, 0.45);
          backdrop-filter: blur(16px);
          -webkit-backdrop-filter: blur(16px);
          border: 1px solid rgba(255, 255, 255, 0.06);
          box-shadow: 0 4px 30px rgba(0, 0, 0, 0.1);
        }
        
        .glow-amber { box-shadow: 0 0 25px -5px rgba(245, 158, 11, 0.35); }
      </style>
    )}

    {tool === 'gif-bolt' && (
      <style is:inline>
        body {
          background-color: #060406 !important;
          background-image: radial-gradient(circle at 50% -25%, #230922 0%, #060406 50%) !important;
          background-attachment: fixed !important;
          color: #e2e8f0 !important;
          min-height: 100vh !important;
          overflow-x: hidden !important;
          width: 100% !important;
        }
        #root { min-height: 100vh; position: relative; z-index: 10; }
        ::-webkit-scrollbar { width: 6px; }
        ::-webkit-scrollbar-track { background: #060406; }
        ::-webkit-scrollbar-thumb { background: #c084fc; border-radius: 10px; }
        ::-webkit-scrollbar-thumb:hover { background: #d946ef; }
        
        .glass-card {
          background: rgba(20, 10, 20, 0.45);
          backdrop-filter: blur(16px);
          -webkit-backdrop-filter: blur(16px);
          border: 1px solid rgba(255, 255, 255, 0.06);
          box-shadow: 0 4px 30px rgba(0, 0, 0, 0.1);
        }
        
        .glow-fuchsia { box-shadow: 0 0 25px -5px rgba(217, 70, 239, 0.35); }
      </style>
    )}
  </head>
  <body>
    <div id="root">
        {tool === 'clipy' && <ClipyApp lang={castLang} dictionary={dictionary} client:load transition:persist />}
        {tool === 'twitchbolt' && <Twitchbolt lang={castLang} dictionary={dictionary} client:load transition:persist />}
        {tool === 'kickbolt' && <Kickbolt lang={castLang} dictionary={dictionary} client:load transition:persist />}
        {tool === 'formatflow' && <Formatflow lang={castLang} dictionary={dictionary} client:load transition:persist />}
        {tool === 'pastesnap' && <Pastesnap lang={castLang} dictionary={dictionary} client:load transition:persist />}
        {tool === 'compresssnap' && <Compresssnap lang={castLang} dictionary={dictionary} client:load transition:persist />}
        {tool === 'backgroundremover' && <Backgroundremover lang={castLang} dictionary={dictionary} client:load transition:persist />}
        {tool === 'pdf-flow' && <Pdfflow lang={castLang} dictionary={dictionary} client:load transition:persist />}
        {tool === 'recordsnap' && <Recordsnap lang={castLang} dictionary={dictionary} client:load transition:persist />}
        {tool === 'qr-bolt' && <Qrbolt lang={castLang} dictionary={dictionary} client:load transition:persist />}
        {tool === 'codecard' && <Codecard lang={castLang} dictionary={dictionary} client:load transition:persist />}
        {tool === 'cropsnap' && <Cropsnap lang={castLang} dictionary={dictionary} client:load transition:persist />}
        {tool === 'css-designer' && <CSSDesigner lang={castLang} dictionary={dictionary} client:load transition:persist />}
        {tool === 'json-flow' && <JSONFlow lang={castLang} dictionary={dictionary} client:load transition:persist />}
        {tool === 'socialbolt' && <SocialBolt lang={castLang} dictionary={dictionary} client:load transition:persist />}
        {tool === 'tts-bolt' && <TTSBolt lang={castLang} dictionary={dictionary} client:load transition:persist />}
        {tool === 'gif-bolt' && <GIFBolt lang={castLang} dictionary={dictionary} client:load transition:persist />}
        {tool !== 'clipy' && tool !== 'twitchbolt' && tool !== 'kickbolt' && tool !== 'formatflow' && tool !== 'pastesnap' && tool !== 'compresssnap' && tool !== 'backgroundremover' && tool !== 'pdf-flow' && tool !== 'recordsnap' && tool !== 'qr-bolt' && tool !== 'codecard' && tool !== 'cropsnap' && tool !== 'css-designer' && tool !== 'json-flow' && tool !== 'socialbolt' && tool !== 'tts-bolt' && tool !== 'gif-bolt' && (
          <div class="min-h-screen bg-[#060609] text-slate-200 font-sans">
             <ToolPlaceholder lang={castLang} toolSlug={tool!} client:load />
          </div>
        )}
    </div>

    <!-- SSR SEO Content for AdSense Approval -->
    <div class="hidden-seo-content" style="display: none;" aria-hidden="true">
      <h1>{finalTitle}</h1>
      <p>{t('seo_description')}</p>
      
      {/* Usar llaves que realmente existen en el diccionario para Clipy y otros */}
      {t('footer_seo_title') && <h2>{t('footer_seo_title')}</h2>}
      {t('footer_seo_paragraph1') && <p>{t('footer_seo_paragraph1')}</p>}
      {t('footer_seo_paragraph2') && <p>{t('footer_seo_paragraph2')}</p>}
      {t('footer_seo_desc_large') && <p>{t('footer_seo_desc_large')}</p>}

      {/* Solo intentar mapear si t.faq es realmente un array */}
      {Array.isArray(t.faq) && (
        <>
          <h2>{t('faqTitle') || 'FAQ'}</h2>
          {t.faq.map((item: any) => (
            <div>
              <h3>{item.question}</h3>
              <p>{item.answer}</p>
            </div>
          ))}
        </>
      )}

      {/* Solo intentar unir si t.seoKeywords es realmente un array */}
      {Array.isArray(t.seoKeywords) && (
        <>
          <h2>{t('seoKeywordsTitle') || 'Keywords'}</h2>
          <p>{t.seoKeywords.join(', ')}</p>
        </>
      )}
    </div>
    <CookieBanner lang={castLang} client:load />
  </body>
</html>
