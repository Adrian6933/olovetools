# Tabla de errores y mejoras — revisión de las 62 herramientas

Inicio: 2026-09-19. Base: `8085ff5`. Se va rellenando según avanza la revisión.

**Leyenda de estado**
- ✅ Funciona como se espera en lo probado.
- 🔧 Tenía un fallo y **ya está arreglado** en esta revisión.
- ⚠️ Funciona, pero hay algo que conviene mejorar o no se ha podido probar del todo.
- ❌ Fallo abierto (no arreglado todavía).

## Resumen

- **Revisadas las 62 herramientas**: pasada automática en todas (carga, hidratación, móvil, textos) y prueba de uso real en todas salvo las partes que exigen permisos del sistema (cámara, micrófono, captura de pantalla, guardar en disco).
- **Fallos arreglados**: 26 filas 🔧 + 9 arreglos transversales (plurales, textos en inglés, descargas en Safari, nombres repetidos en ZIP, `decode()` en pestaña oculta, vídeos WebM sin duración, traducciones sin tildes…).
- **Pendiente de decidir o de hacer** (no arreglado):
  - ❌ json-flow: el detalle de los ~30 mensajes de error del analizador sigue en inglés (necesita plantillas con parámetros en 9 idiomas).
  - ⚠️ backgroundremover: librería con licencia **AGPL-3.0** y modelos servidos desde el CDN de img.ly.
  - ⚠️ codecard: el SVG exportado pesa más que el PNG, al contrario de lo que dice su FAQ.
  - ⚠️ whois-bolt: el nombre promete «Whois» pero solo hace DNS.
  - Mejoras transversales: números con punto decimal en idiomas de coma, 16 textos con «(s)», un único `Footer`/`LegalModal` compartido en vez de más de 50 copias.
- **No probado** (necesita permisos o selectores del sistema): grabar en recordsnap, cámara/micro en device-test, guardar «directo al disco» en framebolt.

## 1. Pasada automática (las 62, en `/es`)

Qué se comprobó en cada herramienta, cargándola en un navegador real a 1280 px y a 375 px:
errores de consola y excepciones al cargar, fallos de hidratación de React, islas que no llegan a hidratar,
desbordes horizontales en móvil, textos que faltan en algún idioma o salen en crudo.

| Comprobación | Resultado |
|---|---|
| Errores de consola / excepciones al cargar | 0 en 61 herramientas; RecordSnap tenía 2 fallos de hidratación → 🔧 arreglados |
| Islas sin hidratar | 0 |
| Desborde horizontal a 375 px | 0 |
| Claves de traducción en crudo en pantalla | 0 (los avisos del detector eran nombres propios: `eFootball`, `utm_source`, `dotLottie`…) |
| Textos que salían en inglés en los 9 idiomas | 19 claves en 5 herramientas + 2 textos de contacto en 16 → 🔧 añadidas (430 entradas) |
| Descargas que liberaban la URL en el mismo clic (riesgo Safari) | 16 puntos en 14 herramientas → 🔧 unificado a liberar a los 60 s, como ya hacían FrameSnap y Lottie |
| Plurales con 1 («1 problemas», «1 coincidencias», «Este enlace lleva 1 parámetros»…) | 24 plantillas sospechosas en 12 herramientas; las 13 que pueden valer 1 → 🔧 arregladas con variante `_one` (json-flow, xml-json, unitflow, exif-clear, html-sanitizer, cron-flow, url-bolt, sql-flow, morse-flow) y «checksum(s)» de hash-bolt reescrito como «etiqueta: número». Las otras 11 nunca valen 1 (límites, «más de 60 capas»…). |
| Archivos con el mismo nombre dentro de un ZIP | JSZip sobrescribe en silencio: dos fotos `IMG_0001.jpg` dejaban **una sola** en el ZIP. Afectaba a exif-clear, watermark-snap y cropsnap (lotes) → 🔧 helper común `src/lib/uniqueName.ts` que numera las repetidas («IMG_0001 (2).jpg»). socialbolt ya lo hacía bien. |
| Textos con «(s)» / «(es)» | Quedan 16 en 8 herramientas («archivo(s)», «capa(s) oculta(s) eliminada(s)» en lottie-viewer, pdf-flow, subtitles-bolt, tts-bolt, watermark-snap, compresssnap, base64-bolt). Mejora de estilo pendiente: variantes `_one` como en los ya arreglados. |
| Números con punto decimal en idiomas de coma | Visto en diffsnap, entropy-bolt, compresssnap, pdf-flow («10.5 KB»): se formatean con `toFixed` en vez de `Intl.NumberFormat`. Mejora pendiente transversal. |
| `img.decode()` con la pestaña oculta | En Chromium no se resuelve para imágenes raster (PNG/JPEG) mientras la pestaña está oculta (con SVG sí). CropSnap se quedaba en «Leyendo la imagen…» si el usuario soltaba una foto y cambiaba de pestaña; background-remover igual en el recorte manual → 🔧 helper `src/lib/loadImage.ts` (espera a `load`, que sí llega, y `decode()` con tope). |
| Vídeos WebM grabados con MediaRecorder (los de RecordSnap y muchas grabaciones de pantalla) | Llegan con `duration = Infinity`. FrameSnap lanzaba un lote de **500 capturas** sobre un vídeo de 2 s (con «hasta 0 = final») y no terminaba; GIF Bolt los **rechazaba como no compatibles**; FrameBolt los daba por de 0 s → 🔧 helper `src/lib/videoDuration.ts` que salta al final para descubrir la duración real (funciona también con la pestaña oculta). |
| Código muerto | 3 ficheros que no importaba nadie (`LegalModal` de backgroundremover y compresssnap, `Footer` de klipy) → 🔧 borrados |

## 2. Tabla por herramienta

| Herramienta | Estado | Errores encontrados | Mejoras propuestas | Cómo se probó |
|---|---|---|---|---|
| recordsnap | 🔧 | El selector de formato se calculaba al renderizar: el servidor pintaba solo WEBM desactivado y el navegador MP4+WEBM, y la fila del códec solo existía en el cliente. React tiraba la hidratación y regeneraba la herramienta entera. Arreglado detectando formatos y códec tras montar. | — | Barrido 1280/375 px: sin errores tras el arreglo. |
| aspect-ratio | 🔧 | El aviso "no cabe un encuadre exacto" solo estaba en inglés y español; en los otros 7 idiomas salía en inglés. | — | Comparación de claves entre idiomas. |
| base64-bolt | 🔧 | El botón "devolver el resultado a la entrada" salía en inglés en los 9 idiomas (clave inexistente). | — | Búsqueda de claves usadas sin definir. |
| clip-flow | 🔧 | Los 11 controles del reproductor (pausa, fotograma a fotograma, velocidad, silencio, pantalla completa) salían en inglés en los 9 idiomas. Descarga con liberación inmediata de la URL. | — | Búsqueda de claves usadas sin definir. |
| pdf-flow | 🔧 | Botones de zoom (alejar, acercar, ajustar) en inglés en los 9 idiomas. | — | Búsqueda de claves usadas sin definir. |
| markdown-live | 🔧 | Título "How it works" en inglés en los 9 idiomas; "Copied!" y el texto de contacto también. | — | Búsqueda de claves usadas sin definir. |
| 15 herramientas con pie propio (audiosnap, css-designer, diffsnap, drawsnap, favicon-bolt, gif-bolt, hash-bolt, json-flow, lottie-viewer, meme-bolt, regex-flow, socialbolt, svg-optimizer, wordflow, zip-flow) | 🔧 | "Copied!" y "Contact for ideas and comments:" en inglés en todos los idiomas. | Unificar ese pie en un solo componente compartido: hay más de 50 copias casi iguales de `Footer.tsx`/`LegalModal.tsx`. | Búsqueda de claves usadas sin definir. |
| clipy / klipy | ⚠️ | — | Todas las preferencias se leen de `localStorage` dentro de un único `try`: si una está corrupta se pierden las que van detrás (bloqueados, idiomas…). Y los `setItem` no están protegidos (modo privado de Safari o cuota llena). | Revisión de código. |
| json-flow | ⚠️ | El detalle de cada error del analizador (unas 30 frases: "Trailing comma before…", "Expected "," or "]" but found…") sale siempre en inglés, aunque la etiqueta de arriba sí está traducida. ❌ abierto: necesita plantillas con parámetros en 9 idiomas. | El CSV no lleva BOM: Excel en español abre mal las tildes y la ñ. Añadir BOM (o una opción "para Excel"). | JSON inválido (coma sobrante → "línea 2, columna 12" y botón Reparar), formatear/minificar con emoji y número de 20 cifras (se conservan), conversión a XML/YAML, tabla, descarga CSV (comillas bien escapadas) y .json. |
| xml-json | ✅ | — | El H1 y el título de la página en español dicen "XML to JSON" (título: «XML to JSON · XML a JSON · …»). | XML con atributos, espacio de nombres, entidad `&amp;`, CDATA y elemento vacío → JSON correcto; XML mal cerrado → 2 errores en español con línea:columna. |
| sql-flow | 🔧 | Decía "1 líneas" y "1 problemas". Arreglado con plurales por idioma (`Intl.PluralRules`, incluidas las 3 formas del ruso). | — | Consulta con JOIN, LIKE con comilla escapada `o''neil`, `;` dentro de texto, comentario y dos sentencias → formateo correcto y descarga .sql; cadena sin cerrar → aviso en 1:27. |
| regex-flow | 🔧 | Los errores de una regex inválida salían en crudo y en inglés, con banderas internas que el usuario no puso («Invalid regular expression: /(abc/dgi: Unterminated group»), aunque el código decía que se traducían. Arreglado: 13 causas reconocidas en Chrome, Firefox y Safari, con explicación en los 9 idiomas (el original queda en el tooltip). Añadido aviso cuando se usa `\p{…}` sin la bandera `u`/`v`, que antes daba «sin coincidencias» sin explicar por qué. | — | Grupos con nombre (`(?<año>…)`), coincidencia vacía `x*` (no se cuelga), Unicode, sustitución `$<m>/$<y> ($$)` → «01/2024 ($)», y 9 patrones rotos distintos → mensaje correcto en cada uno. |
| base64-bolt | 🔧 | El tipo detectado salía en inglés en los 9 idiomas ("PNG image", "ZIP archive", "Word document"… 44 formatos, en 4 sitios de la interfaz). Arreglado con plantillas por clase ("Imagen {0}", "{0}-Bild"). El botón para descargar el archivo decodificado solo decía ".png", sin texto ni descripción: ahora "Descargar .png" con tooltip. | El dibujo de la cabecera tiene textos fijos en inglés ("3 bytes", "4 chars", "+33% size"). | Texto con ñ y emoji → `SG9sYSDDsSDwn5iA` y vuelta; Base64 con `$` → avisa de cada carácter omitido con su posición; PNG 20×10 → data URL → archivo descargado idéntico (152 B, 20×10). |
| url-bolt | ✅ | — | Los textos del dibujo de la cabecera (scheme, host, path, query) están en inglés. | Codificar `a b&c=ñ/?#😀`; decodificar con `+` y `%C3%B1`; entrada rota `hola%E0%A4%A y %ZZ` → decodifica lo que puede y marca 3 fallos con su posición; inspector con usuario:contraseña, puerto, ruta con ñ, parámetro repetido y `utm_source` → avisa de credenciales en claro y del rastreador. |
| hash-bolt | ✅ | — | «1 checksum(s) leídos»: usar singular/plural de verdad. Arranca en modo Archivos y el único cuadro de texto visible es el de verificar: quien viene a hashear un texto puede pegarlo ahí y no ver nada. | "abc" contra los valores de referencia de MD5, SHA-1, CRC-32, SHA-256, SHA-512, SHA3-256, BLAKE2b-256, BLAKE3, RIPEMD-160 y xxHash64 → los 10 correctos; archivo → mismo SHA-256; verificación contra SHA256SUMS → coincide; descarga del fichero de sumas. |
| uuid-generator | ✅ | — | — | Pendiente de la auditoría anterior: UUID v3 y v5 de `python.org` en el espacio DNS → coinciden con los vectores publicados (`6fa459ea-…`, `886313e1-…`). |
| list-mixer | ✅ | — | El orden «A-Z» por defecto pone `item10` antes que `item2`; existe el modo «1-9», pero lo que casi todo el mundo espera es orden natural por defecto. | Lista con tildes, mayúsculas, duplicado, línea vacía y `O'Neil` → orden correcto con tildes, quitar duplicados y salida SQL `IN (…)` con `O''Neil` bien escapado. |
| html-sanitizer | ✅ | — | — | Carga con 11 vectores XSS (onclick, onerror, `javascript:` en enlace e iframe, `<script>` suelto y dentro de SVG, `<style>`, formulario y el truco de mutación `math/mglyph/style`) → los 11 eliminados, nada se ejecutó en la página, conserva ñ y enlace seguro con `rel=noopener`. |
| markdown-live | 🔧 | El ancla «#» de cada título llevaba `aria-label="Link to this section"` fijo en inglés, también en el HTML exportado. Arreglado pasándolo traducido hasta el worker y la exportación. | No soporta fórmulas (`$E=mc^2$` sale tal cual). | Tabla con alineación, lista de tareas, bloque de código, nota al pie, `<img onerror>` (se muestra como texto) y enlace `javascript:` (anulado); descarga .md idéntica y .html autocontenido con `lang="es"` y título del documento. |
| morse-flow | 🔧 | Las vocales con tilde sin código propio (á, í, ó, ú) salían como `#`: «Ñandú» o «canción» quedaban rotas en español, portugués y francés. Arreglado enviando la letra base, como hace cualquier operador; Ñ, É, Ü… conservan su código. «1 caracteres no tienen equivalente» → singular. | — | Texto → Morse con Ñ, tildes y emoji (el emoji sí avisa como `#`), Morse → texto «HOLA MUNDO», descarga WAV válida (cabecera RIFF/WAVE, 531 KB). |
| binary-flow | 🔧 | En modo entero, el punto y la coma se borraban siempre como si fueran separadores de miles: **«12.5» se convertía en 125 sin avisar**. Arreglado: solo cuentan como separador si los grupos lo parecen (3 cifras en decimal, 4 u 8 en hexadecimal y binario); si no, avisa «Un entero no lleva decimales. Para 12,5 cambia a Coma flotante». | En coma flotante, escribir `0.1` en «Valor de entrada» (que espera el patrón de bits) también quita el punto; el decimal va en el otro campo, «Valor decimal». Sería más claro avisar ahí también. | 255 → FF/11111111; 2⁶⁴ sin truncar (BigInt); −1 con signo en 8 bits → FF; 300 en 8 bits → aviso de desbordamiento; 0.1 en float32/64 → `3DCCCCCD` / `3FB999999999999A`; «1.000.000», «1,048,576» y `dead.beef` siguen aceptándose. |
| epoch-flow | 🔧 | Con timestamps negativos en µs o ns la fecha y la fracción no cuadraban: el milisegundo se truncaba hacia cero y el resto se tomaba positivo. −1 ns salía como «1970-01-01T00:00:00.000 + 999 µs» (después de 1970, no antes) y −1500 µs caía en .999 en vez de .998. Arreglado redondeando hacia abajo, como ya hacía el formateo inverso. | — | 0, 1700000000 (s) y 1700000000000 (ms) → 2023-11-14T22:13:20Z y hora de Madrid; −1 y −1500 en s/ms/µs/ns; FILETIME 116444736000000000 → 1970; Excel 45000 → 2023-03-15; día juliano 2451545 → 2000-01-01T12:00Z. |
| cron-flow | 🔧 | `*/1 * * * *` se describía como «Cada 1 minutos» (y lo mismo con horas y segundos). Arreglado: con paso 1 dice «Cada minuto» / «Cada hora». | Con `30 2 * * *` en una zona con horario de verano cuenta «365 ejecuciones al año» y no avisa en el propio resultado de que la noche del cambio de marzo las 02:30 no existen (solo lo explica una FAQ). | Laborables a las 9 → descripción y 261 ejecuciones/año; 31 de febrero → «no se dispara nunca»; día del mes + día de semana → aviso de «CUALQUIERA de los dos»; `61` → «se sale del rango de Minuto (0-59)»; `*/1` → aviso de exceso de ejecuciones. |
| jwt-bolt | ✅ | — | — | Token HS256 de ejemplo de jwt.io: decodifica, «La firma es válida» con el secreto correcto y «La firma no coincide» con otro; token con `alg: none` y `exp` pasado → «Sin asegurar» y «Token caducado». |
| unitflow | ✅ | — | Una entrada que no se entiende (`abc`) solo muestra «—», sin decir qué falla. | −40 °C = −40 °F; 0,1 °C → 32,18 °F / 273,25 K / 491,85 °R; 1 m → 3,28083989501 ft; `5'11"`, `2 ft 3 in`, `3/4`, `2,5` (coma decimal), `1e3`; 1 GB → 0,931 GiB. |
| hex-to-rgb | ✅ | — | — | `#F63`, `#FF634780` (con alfa), `rebeccapurple`, `hsl(120 100% 25%)`, `oklch(70% 0.15 30)`, `color-mix(in srgb, red 50%, blue)` → conversiones correctas; texto que no es un color → «No es un color». |
| wordflow | ✅ | — | — | Texto con signos de apertura, ñ, ü, «e-mail», «3,5 kg» y japonés → 20 palabras (idéntico a `Intl.Segmenter`), 98 caracteres, tiempos de lectura y legibilidad. |
| diffsnap | ✅ | — | Los porcentajes y tiempos salen con punto decimal en español («66.7%», «4.7 ms»): usar el formato del idioma. | Dos versiones con una línea cambiada y otra añadida → +1 / reescrita 1 / sin cambios 3, vista lado a lado; `.patch` descargado es un diff unificado válido (con «No newline at end of file»). |
| subtitles-bolt | 🔧 | Las etiquetas de estilo del SRT (`<i>`, `<b>`, `<u>`, `<font color>`) no se convertían, aunque el código decía que sí: en **TTML se escapaban y el espectador veía «<i>mundo</i>» escrito en pantalla**, y en ASS quedaba el `<i>` literal. Arreglado: ASS usa `{\i1}…{\i0}`, `{\b1}`, `{\c&HBBGGRR&}`; TTML usa `<span tts:fontStyle/fontWeight/textDecoration/color>` y cierra las etiquetas que venían sin cerrar (XML válido). También liberaba la URL de descarga al instante (igual en cron-flow, regex-flow y url-bolt) → 60 s. | El TTML sale con `xml:lang=""`: podría ponerse el idioma detectado o elegido. | SRT con BOM, CRLF, cursiva, solape de 500 ms y milisegundos de un dígito (`00:01:00,5` → .500) → VTT correcto; revisión marca «Se solapa 500 ms con la siguiente»; ASS y TTML con cursiva, negrita, color y etiqueta sin cerrar → formato nativo de cada uno. |
| entropy-bolt | ✅ | — | Números con punto decimal en español («129.8 bits», «3.7bits», sin espacio). | Generar 10+ contraseñas de 20 caracteres → todas distintas, 129,8 bits (cuadra con un alfabeto de 90); comprobar `password123` → «Muy débil», «aparece en listas de filtraciones», «al instante». |
| time-bolt | ✅ | — | Los nombres de ciudad salen en inglés («New York» en la página en español). | Diferencias en torno a los cambios de hora de 2026: 1 mar (Madrid +1, Nueva York −5), 15 mar (EE. UU. ya en verano: −4) y 5 abr (Madrid +2, Londres +1) → todas correctas. |
| key-doctor | ✅ | — | El informe TXT está en inglés en todos los idiomas y mete una frase traducida en medio («Key legends: Símbolos genéricos…»). | Área de captura con foco + pulsaciones → 6/105 teclas, pulsación simultánea y tiempo medio; exportación TXT con teclas no probadas. (Umbral de tecla atascada: 4 s.) |
| graph-flow | 🔧 | Con un CSV exportado por Excel en español (`;`, coma decimal, punto de miles), «1.234,5» se leía bien pero **«1.100» pasaba a 1,1 y «3.500» a 3,5** en el mismo archivo: el gráfico salía mal sin avisar. Arreglado: se detecta la convención del archivo con las celdas que no dejan duda y con ella se desempatan las ambiguas. | — | CSV con `;`, «1.234,5», «2.000,75», «1.100», «"3.500"», «1.250,25» → 1234,5 / 2000,75 / 1100 / 3500 / 1250,25; gráfico en canvas con 7 tipos y panel de datos editable. |
| lorem-flow | ✅ | — | — | 7 palabras, 50 caracteres y 30 caracteres en japonés → cantidades exactas; salida HTML con `<p>`; 8 alfabetos. |
| whois-bolt | 🔧 | Al limpiar lo escrito se quitaba el `www.`, pero es una consulta DNS y `www.example.com` es otro nombre (a menudo un CNAME a una CDN): se respondía sobre algo que no se había preguntado. Tampoco se quitaban el usuario:contraseña ni el puerto (`example.com:8080` fallaba). Arreglado. | El título dice «Whois» pero solo hace DNS (no hay RDAP/registro del dominio): o se añade o se ajusta el nombre para no prometer lo que no da. | `https://user:pw@www.github.com:443/a?b` → consulta `www.github.com` (sale su CNAME); `github.com` → registro A; `example.com` → NS de Cloudflare y DNSSEC validado. |
| formatflow | ✅ | — | El TIFF sale sin comprimir (400×300 → 481 KB); se podría ofrecer LZW/Deflate. | PNG 400×300 → JPG, WEBP, ICO, PDF, TIFF y SVG: cabeceras correctas y mismas dimensiones; AVIF desactivado con explicación porque este navegador no lo codifica (comprobado aparte con `canvas.toBlob`). |
| compresssnap | ✅ | — | Tamaños con punto decimal en español («1.8 MB», «549.4 KB»). | PNG de 1200×900 con ruido (1,8 MB) → JPEG «Para la web» 549,4 KB (−71 %); ZIP descargado con `01_grande-min.jpg`. |
| pdf-flow | 🔧 | En «Extraer páginas», con un rango que no existe («9» en un PDF de 3) no salía ningún aviso **y seguía visible el botón de descarga del resultado anterior**: se bajaba el PDF del rango de antes sin darse cuenta. Arreglado: cambiar el rango descarta el resultado viejo, y un rango sin páginas válidas avisa «Ninguna de esas páginas existe: el documento tiene 3» y desactiva Procesar. | «3,1» se extrae como 1,3 (se ordena): quien escribe un orden probablemente lo quiere así. Al pasar a un modo de un solo archivo se descartan los demás sin avisar. | 3 PNG → PDF (`%PDF-1.7`, 3 páginas); extraer página 1; unir dos PDF de 3 páginas → 6 páginas. |
| qr-bolt | ✅ | — | — | QR WiFi con nombre `Casa;"Ñ"` y contraseña `a\b;c:d` → cadena `WIFI:T:WPA;S:Casa\;"Ñ";P:a\b\;c\:d;;` con los escapes correctos; descargas PNG, SVG y JPEG (cabecera JPEG real) en 1024 px. |
| qr-reader | ✅ | — | — | Lectura del PNG anterior → «Red WiFi», nombre `Casa;"Ñ"` bien desescapado, seguridad WPA y contraseña oculta; «No hemos visto nada sospechoso» sin abrir nada. |
| exif-clear | 🔧 | Dos fotos con el mismo nombre → el ZIP salía con **una sola** (la otra se perdía sin aviso). Arreglado con el helper común de nombres únicos. «Limpiar 1 archivo(s)» → singular/plural real. | — | JPEG con EXIF construido a mano (Canon EOS 5D + GPS de Madrid): muestra «40° 25' N, 3° 42' W»; «Solo la ubicación» → quita el puntero GPS y conserva la marca; «Borrarlo todo» → sin bloque EXIF ni marca; la imagen resultante sigue decodificando a 200×150. |
| zip-flow | ✅ | — | — | Comprimir `nota.txt` ×2 y `año ñ.txt` → «nota (2).txt» sin pisarse y nombre con ñ marcado como UTF-8; extraer ese ZIP (tras pulsar «Abrir comprimido») → 3 entradas y descargas individuales con el contenido correcto. |
| svg-optimizer | ✅ | — | Al quitar un elemento oculto que otro `<use>` referenciaba, deja el `<use href="#b">` apuntando a la nada (no cambia el dibujo, pero es basura). | SVG «de Illustrator» con comentario, metadata, grupos vacíos, decimales largos, degradado por `id`, `<use>` y texto con ñ → 666 → 375 bytes, `viewBox` y referencias al degradado renombradas coherentemente, y rasterizado idéntico al original (0 de 20.000 píxeles distintos). |
| favicon-bolt | ✅ | — | — | Pack generado desde emoji → `favicon.ico` con 16/32/48, PNG 16, 32, 180, 192, 512 y maskable 512 (cada uno del tamaño que dice su nombre), `favicon.svg`, `site.webmanifest` JSON válido con sus iconos y el fragmento `<head>`. |
| colorsnap | ✅ | — | — | Imagen de 5 bandas (#E63946, #F1FAEE, #A8DADC, #457B9D, #1D3557) → la paleta devuelve exactamente esos 5 colores. |
| meme-bolt | ✅ | — | — | Plantilla «Dos Botones» → PNG a 2× (1800×2300) y JPG a 1× (900×1150), nombres con el tamaño. |
| gif-bolt | ✅ | — | — | 3 PNG de 160×120 → «Generar GIF» → `GIF89a`, 160×120, 3 fotogramas y bucle infinito (NETSCAPE2.0). |
| lottie-viewer | 🔧 | La opción «Quitar metadatos del autor» (activada por defecto) borraba los **nombres de capa** (`nm`), que lottie-ios/android usan como keypaths para cambiar texto o colores en tiempo de ejecución, y además `cl` y `ln`, que el reproductor web convierte en la clase y el id de cada capa del SVG (CSS/JS que dependen de ellos dejaban de funcionar). Arreglado: `cl`/`ln` se conservan siempre, la opción se llama «Quitar nombres de capa (rompe keypaths y texto dinámico en apps)» y viene desactivada. | El PNG de un fotograma sale a 512×512 aunque la animación sea de 200×200 (revisar si es intencionado). | Lottie mínimo (200×200, 30 fps, 60 fotogramas, capa oculta) → 2 capas, 2,00 s; optimizar −3 %; export PNG y JSON. |
| tts-bolt | ✅ | — | — | «Hola, esto es una prueba con eñe.» con voz neuronal (vía `/api/tts`) → audio de 0:03; descargas MP3 (trama MPEG válida), WAV (RIFF/WAVE) y SRT con tiempos del motor; nombres de archivo con ñ. |
| audiosnap | ✅ | — | El `<input type=file>` solo aparece al pulsar «Abrir un archivo»; arrastrar el archivo sobre la zona inicial no hizo nada en la prueba. | WAV de 2 s (1 s de tono 440 Hz + 1 s de silencio) → editor 2,00 s mono; «Recortar el silencio» → 1,12 s; «Renderizar y descargar» → `tono-cut.wav` que decodifica a 1,124 s / 44,1 kHz / mono. |
| codecard / pastesnap (textos) | 🔧 | **Traducciones escritas sin diacríticos**: el español de CodeCard entero sin tildes, eñes ni signos de apertura («Por que otras herramientas…?», «Tamano del tabulador», «Anade una marca de agua»), y lo mismo en francés, portugués y alemán de CodeCard y PasteSnap («ecrire», «so quando voce», «Einfuegen», «Groesse»). Incluye FAQ y textos SEO. Detectado midiendo la proporción de letras acentuadas por archivo (estaban muy por debajo de la mediana). Arreglado con mapas palabra a palabra revisados en contexto: ~53 líneas en es, 156 palabras en fr, 186 en pt, 88 en de (ä/ö/ü/ß sin tocar *aktuell*, *dass*…), más «à», «é» y signos «¿». | — | Proporción de acentos tras el arreglo: es 17,9 / fr 23,2 / pt 22,8 / de 10,1 (medianas: 15 / 26,8 / 24,9 / 12,1); los 8 archivos siguen siendo válidos. |
| codecard | ⚠️ | — | El SVG exportado pesa 1,37 MB (el PNG a 2×, 519 KB) mientras la FAQ dice que «suele pesar menos que un PNG a 4×»: parece incrustar las fuentes completas; subconjuntar la fuente o cambiar el texto. El nombre del archivo sale del título de ejemplo («fetch-user») aunque el código sea otro. Al exportar salen 2 errores en consola por intentar leer el CSS de Google Fonts (otro dominio). | Código con ñ, emoji y una línea muy larga → PNG 2348×506 y SVG con la línea completa; textos de «Siguiente» ya con tildes. |
| cropsnap | 🔧 | Con la pestaña oculta se quedaba para siempre en «Leyendo la imagen…» (ver fila transversal de `decode()`). El ZIP del lote perdía una imagen si dos se llamaban igual. Ambos arreglados. | — | 2 PNG «IMG_0001.png» de 800×600 → editor con 10 proporciones y 10 tamaños de destino; «Lote de 2 (ZIP)» → `IMG_0001-704x528.png` y `IMG_0001-704x528 (2).png`. |
| drawsnap | ✅ | — | Nombre de archivo con marca de tiempo en milisegundos (`drawsnap-1789849825150.png`): uno con fecha legible sería más útil. | Rectángulo y elipse dibujados con eventos de puntero → SVG con `<rect>` y `<ellipse>`, PNG a 3840×2160. |
| whiteboard-flow | ✅ | — | — | Plantilla Kanban → JSON (4 notas, layout kanban), Markdown con listas de tareas por columna y «Hecho» marcado `[x]`, PNG 1664×752; reimportar el JSON devuelve el tablero. |
| css-designer | ✅ | — | — | Efecto cristal por defecto → CSS válido para el navegador (1 regla, todas las propiedades), Tailwind con valores arbitrarios equivalentes, SCSS y variables CSS; PNG 1600×900. |
| socialbolt | ✅ | — | Una línea que no es un enlace («esto no es un enlace») se ignora sin decir nada; mejor marcarla como no válida. | Enlace público de TikTok → título, autor y opciones (vídeo HD, sin marca de agua, MP3, portada) resueltas vía `/api/social`; enlace de YouTube → explica que solo hay metadatos y miniaturas y por qué. |
| pastesnap | ✅ | — | — | Pegar una imagen (evento `paste`) → aparece con 320×200; AVIF desactivado con explicación porque el navegador no lo codifica; convertir a WEBP → «Descargar Todas» da un ZIP con `01_image.webp`, `02_image.webp` (RIFF/WEBP válidos). |
| framesnap | 🔧 | Con un WebM grabado en el navegador, el lote «cada 1 s, hasta 0 = final» calculaba 500 capturas y no terminaba (ver fila transversal). Arreglado: 2 capturas (0 s y 1 s) para un vídeo de 1,9 s. | «fps desconocidos» en esos WebM: el dato no está en el archivo, pero podría medirse igual que en los MP4. | WebM VP8 de 320×240 y 60 fotogramas generado en el navegador → carga, captura suelta, lote correcto tras el arreglo. |
| framebolt | ⚠️ | Con WebM de MediaRecorder daba duración 0 → 🔧 (fila transversal). | «Fotogramas: ?» y «Tamaño del ZIP: ?» cuando el vídeo no trae fps: estimar con los fps medidos o con los de «Cada N segundos». | Carga del WebM → 320×240 y 00:00:00.000 → 00:00:01.915 tras el arreglo. **No probado a fondo**: la extracción escribe «directo al disco» con el selector de archivos del sistema, que la automatización no puede contestar; el modo «Todos» además exige la pestaña en primer plano (la herramienta lo avisa). |
| clip-flow | ✅ | (Controles del reproductor en inglés → 🔧 en la pasada de textos.) | En un directo el contador marca «00:00 / 00:00»; mejor mostrar el tiempo transcurrido de la grabación. | Texto que no es URL y enlace de YouTube → «no parece ser un VOD o canal de Twitch válido»; VOD inexistente → «No se pudo encontrar este VOD»; canal en directo (`rocketleague`, sacado de la API en ese momento) → carga el vídeo, título, calidades Source/720p60 y controles fotograma a fotograma ya en español. |
| cleansnap | ✅ | — | — | Foto de rayas horizontales con un círculo rojo → selección rectangular + «Rellenar» en modo Parches → la zona seleccionada queda con las rayas originales exactas (colores #DDDDEE / #AAAACC y bordes en su sitio), sin mancha borrosa; PNG 400×300. |
| backgroundremover | ⚠️ | (Recorte manual colgado con la pestaña oculta por `img.decode()` → 🔧 fila transversal.) | **Licencia**: `@imgly/background-removal` es **AGPL-3.0**; usarla en una web pública suele obligar a publicar el código fuente de la aplicación que la incorpora (img.ly vende licencia comercial). Además los modelos (20/44/88 MB) se descargan de `staticimgly.com`: si ese CDN cae o cambia, la herramienta deja de funcionar. Valorar alojar los modelos o cambiar a un modelo con licencia permisiva. | Círculo rojo sobre blanco, calidad «Rápida» (WebGPU) → PNG 300×300 con fondo transparente (alfa 0 en esquina y borde) y objeto opaco (alfa 233). |
| device-test | ✅ | — | — | Pestaña Teclado → 3 teclas registradas con `code`/`key`; Puntero cargado. **No probado**: cámara, micrófono, altavoces y pantalla completa (necesitan permisos del navegador que la automatización no debe conceder). |
| aspect-ratio | ✅ | (Aviso «no cabe un encuadre exacto» sin traducir → 🔧 en la pasada de textos.) | — | Sin errores al cargar en escritorio ni móvil; la lógica de proporciones y encuadre ya se validó en la auditoría del 14-sep (`docs/ESTADO-HERRAMIENTAS.md`). |
| twitchbolt | 🔧 | Con un clip que no existe se quedaba **«Cargando clips…» para siempre** y el botón del ZIP bloqueado: probaba todos los relés × clientes y cada `fetch` iba sin tiempo límite; además, cuando Twitch confirmaba `clip: null` seguía preguntando a los demás relés. Arreglado: tope de 10 s por petición y parada inmediata si Twitch confirma que no existe → «Error al cargar el clip» en 1 s. | — | 2 clips reales de las últimas 24 h (sacados de la API) → 1080p60 / 720p60 / 480p30 cada uno; clip inventado → error en 1 s tras el arreglo. |
| kickbolt | 🔧 | Mismo patrón que TwitchBolt: las peticiones para resolver el clip y leer la lista HLS no tenían tiempo límite → tope de 10 s (las descargas de segmentos se dejan sin tope). | — | Ver fila de klipy. |
| klipy | ✅ | — | (Lectura de preferencias en un solo `try`: ver fila clipy/klipy.) | Portada con categorías de Kick (GTA V, Just Chatting, IRL…) → entrar en GTA V carga los clips destacados; un clip real de esa categoría («M1 goes full british», de la API de Kick) se resuelve en KickBolt con su canal; un clip inventado da «Error al cargar el clip» en 18 s. |
| recordsnap | 🔧 | (Hidratación rota → arreglada, ver arriba.) | — | **No probado**: grabar necesita permiso de captura de pantalla/cámara, que la automatización no debe conceder. |
